algorithme NomAlgorithme

variables
    variable : entier
    c : caractère

début
    c = 'A'
    écrire("Le caractère est:", c, " et son code ASCII est:")
    écrireln(ord(c))
    pour i de 0 à 10 faire
        écrireln(i)
    finpour
fin