algorithme TestRapideOrdChr

variables
    c : caractere
    code : entier

debut
    // Test simple
    c = 'A'
    code = ord(c)
    ecrire("ord('A') = ", code)
    ecrireln()

    c = chr(66)
    ecrire("chr(66) = '", c, "'")
    ecrireln()

    ecrireln("✓ Fonctions ord() et chr() fonctionnent!")
fin

